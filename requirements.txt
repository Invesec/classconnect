Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Mail==0.10.0
Flask-SocketIO==5.3.6
Werkzeug==3.0.4
python-dotenv==1.0.1
requests==2.32.3
# Postgres driver — required when DATABASE_URL points at a real Postgres
# database (e.g. Neon's free tier) instead of the local SQLite fallback.
psycopg2-binary==2.9.9
# PDF and Word document generation for attendance export
reportlab==4.2.5
python-docx==1.1.2
# Required in production: Render's Start Command runs
# `gunicorn --worker-class eventlet -w 1 app:app`, so both of these must be
# actual installed dependencies, not commented out — without them the build
# fails with "gunicorn: command not found" (exit code 127).
gunicorn==23.0.0
eventlet==0.35.2
# Uncomment for MySQL instead of default SQLite:
# PyMySQL==1.1.1
